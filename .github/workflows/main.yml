name: Builld and Deploy SFTP
on:
  push:
    branches: ['main']

concurrency:
  group: deploy-test
  cancel-in-progress: true

jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - uses: actions/setup-node@v4
        with:
          node-version: latest
          cache: npm
      - name: Check required secrets
        run: |
          if [ -z "${{ secrets.SFTP_HOST }}" ]; then echo "SFTP_HOST is missing"; exit 1; fi
          if [ -z "${{ secrets.SFTP_USER }}" ]; then echo "SFTP_USER is missing"; exit 1; fi
          if [ -z "${{ secrets.SFTP_PASSWORD }}" ]; then echo "SFTP_PASSWORD is missing"; exit 1; fi
          echo "Secrets are set"
    
      - name: Install
        run: npm ci

      - name: Build (Astro)
        run: npm run build

      - name: Upload pawequira.de
        uses: pressidium/lftp-mirror-action@v1
        with:
          host: ${{ secrets.SFTP_HOST }}
          port: 22
          user: ${{ secrets.SFTP_USER }}
          pass: ${{ secrets.SFTP_PASSWORD }}
          onlyNewer: true
          settings: 'sftp:auto-confirm yes'
          localDir: './dist/'
          remoteDir: './pawequira.de/'
          options: '--delete'
